@model IEnumerable<CinephilesChoice.Models.Nomination>

@{
    ViewData["Title"] = @Model.First().Year + " - " + @Model.First().AwardCategory;
    Layout = "~/Views/Shared/_Layout_Cinephile.cshtml";
}

    <h1>@Model.First().Year - @Model.First().AwardCategory</h1>
    <a asp-action="VoteOnNomination" asp-route-year="@Model.First().Year" asp-route-category="@Model.First().AwardCategory">Vote</a>

<div class="row">
    @foreach(Nomination nomination in Model)
    {
    <div class="col-md-4 movie-poster-container">
        <img class="movie-poster" src="@nomination.Movie.Poster" alt="@nomination.Movie.Title" />
        <div class="movie-poster-overlay">
            <div>
                <h5>Nominee: @nomination.Nominee</h5>
                <h6>Title: @nomination.Movie.Title</h6>
                <h6>Directed by: @nomination.Movie.Director</h6>
                <h6>Starring: @nomination.Movie.Actors</h6>
            </div>
        </div>
    </div>
    }
</div>
